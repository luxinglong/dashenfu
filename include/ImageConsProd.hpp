#pragma once
#include "opencv2/opencv.hpp"
#include <string>

class ImageConsProd{
public:
	//ImageConsProd(Settings * _settings,){}
	ImageConsProd();
	void ImageProducer();
	void ImageConsumer();		

public:
	//Settings * settings;
	//OtherParam * other_param;
	//int fd2car;
};
